# Clean Build

Clean the project build artifacts.

## Instructions

Run clean command:

```bash
./gradlew clean
```

This will remove:
- Build directories
- Generated files
- Cached artifacts

After cleaning, optionally rebuild with `./gradlew assembleDebug` if requested.
